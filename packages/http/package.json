{
  "name": "@risk-engine/http",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "tsc -b",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc -b --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "peerDependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "typescript": "^5.6.0"
  }
}
