{
  "name": "@risk-engine/events",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "tsc -b",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc -b"
  },
  "dependencies": {
    "@risk-engine/types": "workspace:*"
  }
}

