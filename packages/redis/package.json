{
  "name": "@risk-engine/redis",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "tsc -b",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc -b --noEmit"
  },
  "dependencies": {
    "@socket.io/redis-adapter": "^8.0.0",
    "bullmq": "^5.0.0",
    "ioredis": "^5.4.0"
  }
}

