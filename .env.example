NODE_ENV=development

# Postgres / Redis
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/incident_intel
REDIS_HOST=localhost
REDIS_PORT=6379

# API gateway
API_GATEWAY_PORT=4000

# WebSocket service
WS_SERVICE_PORT=4001

# Worker service
WORKER_PORT=4002

# Ingestion service
INGESTION_PORT=4100

# Web (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=http://localhost:4001
# After creating an org + project + API key via POST /organizations → POST /projects → POST /projects/:id/api-keys:
NEXT_PUBLIC_ORGANIZATION_ID=
NEXT_PUBLIC_API_KEY=

# Redis stream & queue names
REDIS_STREAM_NAME=platform-events
ANOMALY_QUEUE_NAME=anomaly-detection

# Webhook secrets (optional)
STRIPE_WEBHOOK_SECRET=
WEBHOOK_SECRET=

# Email (Brevo — https://brevo.com)
BREVO_API_KEY=xkeysib-...
DASHBOARD_URL=http://localhost:3000

# Flags
SIGNUP_DISABLED=true
